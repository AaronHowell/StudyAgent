"""Reserved command policy boundary for reproduction work.

The concrete runner policy currently lives under ``integrations.sandbox``.
Reproduction code should import this module once command policy is promoted to
the workflow layer.
"""
