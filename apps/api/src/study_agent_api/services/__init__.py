"""Service-layer modules for API-specific orchestration.

Document processing logic was moved into `packages/documents` so that the API
layer stays thin and reusable logic lives in a dedicated package.
"""
