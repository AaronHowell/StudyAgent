# Documents Package

这个包专门承载“文档进入系统之前和之中的处理逻辑”。

当前包含：
- 项目目录扫描
- 文档发现
- PDF 元数据与页面文本提取边界
- 文本切块

这样拆分的目的：
- 避免把 API 壳层和文档处理逻辑混在一起
- 让后续 CLI、批处理脚本、后台任务都能复用同一套文档链路
- 让文档相关代码有单独的学习和演进空间

建议职责边界：
- `apps/api`：暴露接口，组装调用
- `packages/domain`：定义数据结构和协议
- `packages/documents`：实现文档扫描、解析、切块
